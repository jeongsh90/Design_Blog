## 하네스: 디자인 블로그 (티스토리)

**목표:** 디자인(로고·폰트·디자인자료 아카이브) + 코드(프론트엔드·퍼블리싱) + AI(프롬프트·스킬 자료 아카이브) 3개 축의 콘텐츠를 기획·작성·편집하고, 티스토리(외부 호스팅 퍼블릭 블로그 플랫폼)에 바로 발행 가능한 형태(HTML/마크다운 + 첨부자료 + SEO 메타)로 만들어 낸다. 이 프로젝트는 자체 웹앱/서버를 만들지 않는다 — 콘텐츠 제작과 티스토리 퍼블리싱 준비까지가 산출물이며, 실제 발행 버튼은 사용자가 직접 누른다.

**전제:** 완전 공개 퍼블릭 블로그. 자료(로고/폰트/프롬프트 등)는 별도 저장소 없이 티스토리 게시글 첨부파일로 배포. D:\MyCloud 루트의 파일 저장소 앱(storage-app-orchestrator)과는 완전히 별개의 독립 프로젝트. **대시보드 스킨(`dashboard-skin/`) 소스 HTML/CSS/JS에는 주석을 넣지 않는다** — 구현 의도·함정은 같은 경로의 짝 `{파일명}.md`에 적는다(상세: `tistory-skin-development`). 스킨 UI를 바꾸면 Playwright 검증(브라우저 설치·MCP·verify 스크립트)까지 에이전트가 끝낸다. 사용자에게 확인을 넘기지 않는다.

**트리거:** 이 디자인 블로그의 글 기획/작성/편집/발행 준비, 디자인·코드·AI 자료 아카이브 콘텐츠, 티스토리 포스팅 관련 작업 요청 시 `design-blog-orchestrator` 스킬을 사용하라. 후속 수정·재작성·특정 글만 다시·시리즈 기획·카테고리 재구성 요청도 동일 스킬로 처리한다. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-09-01 | 초기 구성 — 5개 에이전트(blog-strategist/design-content-writer/code-content-writer/ai-content-writer/tistory-editor) + 5개 실무 스킬 + 오케스트레이터 스킬. 실행 모드는 서브 에이전트(이 환경에 TeamCreate/TaskCreate 미탑재 — Agent 도구 + 파일 기반(_workspace) 핸드오프로 파이프라인·팬아웃 구성) | 전체 | "디자인+코드+AI 자료 아카이브 디자인 블로그" 신규 구축 요청. 사용자가 공개범위=완전공개 퍼블릭, 기술스택/자료저장/배포=전부 티스토리로 확정 |
| 2026-09-01 | 스킨(테마) 개발 하네스 확장 — 2개 신규 에이전트(tistory-skin-designer/tistory-skin-developer) + 2개 신규 스킬(blog-skin-visual-spec/tistory-skin-development) + 신규 오케스트레이터(tistory-skin-orchestrator, 요구사항 원문 보관용 references/skin-requirements.md 포함). 사용자가 실측 요구사항을 상세히 제공한 직후 `D:\MyCloud\Design_Blog\daitnu-skin-v1.01`(요구사항 대부분을 이미 구현한 완성 스킨 패키지)의 존재를 알려와, 작업 성격을 "빌드"에서 "리스킨"(기존 패키지의 시각 스타일만 D:\MyCloud\2026포트폴리오\Design-system 토큰으로 교체)으로 재정의 — 실측 감사 결과(다크모드 `.dark` 토글 이미 존재, style.css에 CSS 변수 0개로 색상 전부 하드코딩, 원작 라이선스상 재배포/상업적 사용 금지)를 references에 기록 | agents/tistory-skin-{designer,developer}.md, skills/{blog-skin-visual-spec,tistory-skin-development,tistory-skin-orchestrator}/ | "디자인 스킨먼저 만들꺼야" + 상세 기능 스펙(반응형/다크모드/헤더/리스트 3종/커버/TOC 등) 2회 메시지 + "daitnu-skin-v1.01 여기에 있는 컴포넌트 내용을 Design-system로 교체" 요구사항 |
| 2026-09-01 | 감사 오류 정정 — 베이스 스킨 `skin.html`이 실제로는 이미 Pretendard 변수 폰트를 jsdelivr CDN(`orioncactus/pretendard`, 공식 저장소)으로 정상 로드 중임을 재확인("@font-face 없음/미적용"이라던 직전 감사는 grep 범위 실수로 인한 오판이었음). 사용자가 대안으로 눈누(projectnoonnu) CDN 정적 굵기별 방식을 제시해 3가지 후보(①현행 유지 ②Design-system self-host ③눈누 CDN)를 확인 질문으로 제시 → **사용자가 ① 현행 유지로 확정** — 폰트 관련 작업은 하지 않는 것으로 관련 스킬 3곳(references/skin-requirements.md, tistory-skin-orchestrator, tistory-skin-development)과 memory를 정정·확정 | skills/tistory-skin-orchestrator/references/skin-requirements.md, skills/{tistory-skin-orchestrator,tistory-skin-development}/SKILL.md | 사용자가 `font-family:'Pretendard'; src:url('.../projectnoonnu/pretendard@1.0/...')` 스니펫 제시 → "현행 유지 — orioncactus 변수폰트 CDN" 선택 |
| 2026-09-01 | 스킨 리스킨 Phase 2 완료 — tistory-skin-designer(general-purpose로 대신 호출)가 베이스 스킨 실측 감사 후 `_workspace/10_skin-designer_visual-spec.md`(컬러 토큰 15개×2테마 교체표, 컴포넌트별 상세 스펙, 요구사항 판정표) + `_workspace/10_mockup.html`(Playwright로 라이트/다크·3단 반응형·TOC 검증 완료) 제출. 최초 감사가 또 한 번 틀렸음을 발견(실제로는 `:root`/`.dark`에 토큰 15개가 이미 존재 — 재도색 범위가 예상보다 훨씬 작음) + 실버그 다수 발견(body font-family 무효 선언, Top버튼 노출조건 없음, TOC 17개에서 잘림, 원형 썸네일 타원화, wide 이미지 opacity:0 등) + 모호 항목(Q1 배너 교차노출/Q2 액센트색/Q3 태블릿 브레이크포인트/Q4 썸네일 열수/Q5 코드하이라이팅/Q7 대표이미지 전역·개별) 전부 사용자 확인 완료(전부 스펙 제안대로 확정 — 지그재그+개선/Design-system 파랑/768~1023/1280↑4열/highlight.js 도입/전역설정 유지) | _workspace/10_skin-designer_visual-spec.md, _workspace/10_mockup.html | Phase 2 실행 결과 + 4건 확인 질문에 대한 사용자 응답 |
| 2026-09-01 | 스킨 리스킨 Phase 3 완료 — tistory-skin-developer(general-purpose로 대신 호출)가 `skin/`에 실제 리스킨 산출물 완성. 컬러/타이포 15개+ 토큰 교체를 style.css 끝의 별도 오버라이드 블록으로 분리(원본 대조 용이), §9 마크업/JS 버그 10건 전부 수정, highlight.js 도입, Playwright로 7페이지×3뷰포트×라이트다크 실측 검증(가로 오버플로 0건, TOC 19개 정확 매칭 등). 검증 중 스펙에 없던 실버그 5건 추가 발견·수정(모바일 코드블록 오버플로, 숨겨진 이전/다음글 카드가 TOC 버튼 클릭을 가로챔 등). highlight.js 테마를 CDN 대신 style.css에 직접 정의(다크모드 분기가 구조적으로 불가능해서)하는 등 의도적 편차 2건은 보고서에 명시. 티스토리 계정이 없어 서버 렌더링(메뉴바 주입/구독버튼/댓글/광고 등)은 미검증 상태로 명시 | skin/(skin.html,style.css,script.js,index.xml,README.md), skin.zip, _workspace/11_skin-developer_verification.md | Phase 3 실행 결과 |
| 2026-09-01 | 스킨 방향 대전환 — daitnu-skin-v1.01 리스킨(1차)을 폐기(파일은 `skin/`/`skin.zip`에 보존, 더 이상 베이스로 안 씀)하고, **shadcn/ui 방식(Tailwind CSS + CSS 변수 + data-slot + calc(var(--spacing)*n)) 대시보드형 스킨**을 처음부터(greenfield) 구역(컴포넌트) 단위로 만드는 것으로 재정의. 배포 방식도 "스킨 zip 등록"에서 "스킨편집 > HTML편집 > 파일업로드"(`/manage/design/skin/edit#/source/file`)로 변경 — tailwind.css는 Node 빌드가 없는 Tistory 환경 특성상 이 프로젝트 로컬에서 Tailwind v4 CLI로 빌드 후 정적 파일로 업로드. 레이아웃은 100vw 전체화면, PC 우선 구성 후 반응형. 오케스트레이터를 "1회성 감사→스펙→구현 파이프라인"에서 "구역별 반복 루프"로 재설계, 새 근거문서 `references/dashboard-shadcn-requirements.md` 신설(1차 `skin-requirements.md`는 SUPERSEDED 표시 후 Tistory 템플릿 태그 문법 참고용으로만 보존). WebFetch로 shadcn/ui 공식 sidebar.tsx와 globals.css를 실측해 CSS 변수/data-slot/상태로직(쿠키 저장, Cmd+B 단축키, 모바일 Sheet 전환 등)을 문서에 기록 — 첫 구역으로 sidebar 작업 시작 | skills/tistory-skin-orchestrator/{SKILL.md,references/dashboard-shadcn-requirements.md(신규)}, skills/{blog-skin-visual-spec,tistory-skin-development}/SKILL.md, agents/tistory-skin-{designer,developer}.md | "기존 잔재 속성들 모두 제거하고... 블로그를 대시보드 느낌으로... shadcn/ui 에서 제공하는 방식... 일단 구역별 하나씩 만들어갈테니 하네스 내용을 좀 바꾸고... bunx --bun shadcn@latest add sidebar 똑같이 만들어" 요구사항 |
| 2026-09-01 | 대시보드 스킨 Sidebar 구역 — 스펙 완료(tistory-skin-designer, general-purpose 대신 호출) 및 실제 구현 착수. 핵심 발견: `D:\MyCloud\2026포트폴리오\Design-system\css\components.css`(368~789행)에 **shadcn Sidebar 바닐라 CSS 포트가 이미 존재**(`js/components.js` 28~57행에 토글 JS도 존재) — 새로 작성하지 않고 이를 이식 기반으로 삼기로 함(trigger/rail/inset/input/menu-badge/separator/전환애니메이션 7가지만 신규). 색상 형식은 hex로 확정(Design-system hex와 shadcn 원본 oklch가 실측 대조 결과 동일한 색이라 변환 실익 없음). 확인 필요 5건(Q1 폭/Q2 카테고리 메뉴 구현방식/Q3 헤더높이 공유/Q4 푸터 구성/Q5 활성표시) 중 Q2(티스토리 `[##_category_list_##]`가 고정 마크업이라 shadcn data-slot을 못 붙이는 문제 — A정적하드코딩/B DOM재작성/C하이브리드)가 유일한 실질 쟁점이었음 — 사용자가 "Design-system이 shadcn로 만든 것이니 그 파일 보고 적용"으로 응답, 기존 포트를 그대로 쓸 수 있는 A(정적 하드코딩)로 해석해 확정. 나머지는 설계자 제안값 그대로 진행. Sidebar 실제 구현(dashboard-skin/)을 백그라운드로 착수(PC 뷰포트만, 반응형은 다음 단계) | _workspace/sidebar_designer-spec.md, dashboard-skin/(진행중) | "sidebar 컴포넌트 똑같이 만들어" 요청에 대한 스펙 완료 + Q2 확인 질문에 대한 사용자 응답 |
| 2026-09-01 | 대시보드 스킨 Sidebar 구역 — 구현·검증 완료(tistory-skin-developer, general-purpose 대신 호출). Design-system 기존 포트를 그대로 이식(변경분은 `[SPEC …]` 주석으로 원본 대조 가능), 누락 7건(전환애니메이션/trigger/rail/inset/input/menu-badge/separator) 신규 작성, Q2=A(정적 하드코딩)로 카테고리 구현. Tailwind v4 로컬 빌드(`bun run skin:build`) 신규 구성. Playwright로 data-slot 23개/색상/치수/trigger·rail·Ctrl+B 토글/쿠키 복원/FOUC 없음(sidebar.js 제거한 사본으로 실측)/hover-active 구분/활성표시 전부 실측 검증. 스펙과 다르게 한 것 5건 사유 명시(FOUC 스크립트 2겹 추가, 활성표시에 JS 25줄 필요, 검색은 자체 폼, 다크 accent-foreground는 스펙 리터럴 우선, 배지 숫자 0은 실측값). 티스토리 서버 렌더링(방문자수/검색 실동작/광고/관리 메뉴바 충돌)은 계정 없어 미검증 | dashboard-skin/(skin.html,tailwind.css,components/sidebar.{css,js},README.md), _workspace/sidebar_developer-verification.md | Phase 3 실행 결과 |
| 2026-09-02 | 대시보드 스킨 Header 구역 — 스펙+구현 완료(general-purpose 대신 호출, 범위가 좁아 스펙·구현 한 번에 진행). `Design-system/index.html`(292~319행) + `css/layout.css`(23~64행)의 실제 헤더 구조를 그대로 이식(`header`/`header-inner`/`header-start`/`header-actions`, 56px, sidebar-header와 높이 일치 확인). 우측 끝에 홈/태그/방명록 `data-size="sm"` ghost 버튼 3개(아이콘+텍스트, 사이드바 "둘러보기" 항목과 아이콘·링크 재사용) + 테마 토글(PC 숨김, 원본 규칙) 배치. 좌측엔 sidebar-trigger + 2단 브레드크럼(`[##_title_##]`/`[##_page_title_##]`, 동일값이면 접힘). Button/Breadcrumb 컴포넌트도 함께 포팅(공용 프리미티브, 추후 button.css로 분리 예정). 의도적 편차 3건(조상 슬롯 sidebar-inset로 교체(layout.css가 "외부 사용 금지"라 shadcn 정본 따름)/스크롤모델을 헤더sticky+문서스크롤로/트리거 28px 유지(shadcn size-7 원본과 더 일치)). Playwright로 라이트다크·펼침접힘·긴 제목 90자 말줄임·가로오버플로 0·hover/focus-visible·탭순서 전부 실측 검증. `--secondary`/`--destructive` 등 미정의 토큰 있는 Button 변형은 이식 보류. 확인 필요 4건(치환자 실제값/타 치환자 경로/관리메뉴바 충돌/카테고리 브레드크럼 원할 시 확장) 명시 | dashboard-skin/components/header.{css,js}, skin.html·tailwind.css·README.md 갱신, _workspace/header_{designer-spec,developer-verification}.md | "헤더 우측 끝에 홈,태그,방명록 버튼 sm사이즈로 배치하고 디자인시스템에 적용된 헤더 동일하게" 요구사항 |
| 2026-09-02 | 대시보드 스킨 후속 수정 2건 — 소규모라 별도 서브에이전트 없이 직접 처리 후 Playwright로 즉시 검증. (1) 사이드바 푸터의 다크/라이트 토글 버튼 제거(방문자 수는 유지). (2) 헤더 우측 끝에 `data-variant="default"` 즐겨찾기 버튼 신규 추가(별 아이콘, `localStorage` 토글, 목록 화면은 아직 없음). (3) 곧이어 사용자가 "다크/라이트 버튼을 화면 우측 하단에 fixed로"라고 재지시해, 헤더에서 테마 토글을 완전히 빼서 화면 우측 하단 고정 원형 버튼(FAB, `--shadow-md` 신규 토큰)으로 재배치 — 사이드바 푸터·헤더 두 곳에 있던 토글이 이 FAB 하나로 통합됨. 로컬 서버(`bun run skin:serve`)로 실제 브라우저 스크린샷 찍어 라이트/다크 전환, 즐겨찾기 토글+localStorage 반영까지 실측 확인 | dashboard-skin/skin.html, dashboard-skin/components/header.{css,js}, dashboard-skin/src/input.css | "사이드메뉴하단 다크/라이트모드 버튼 제거", "헤더 우측상단 증겨찾기 버튼 '디폴트' 추가", "헤더에있는 다크모드, 라이트모드버튼 화면 우측 하단에 픽시드 배치" 요구사항 |
| 2026-09-02 | git 저장소 초기화 — 이 프로젝트가 그동안 버전관리 없이 파일로만 진행돼 있었음. `git init` + `.gitignore`(node_modules/, .playwright-mcp/ 캐시만 제외, 나머지는 zip 포함 전부 포함) + 초기 커밋(90개 파일). 원격(GitHub) 푸시는 보류 — `daitnu-skin-v1.01`(당시 JQ-minimal-skin-v1.1) 라이선스가 재배포 금지라 원격을 만든다면 반드시 비공개로 해야 한다고 안내, 사용자 확답 대기 중 | .gitignore(신규), .git/ | "깃으로 작업하게 깃에 올려 (모든 내용 파일 포함)" 요구사항 |
| 2026-09-02 | 베이스 스킨 폴더명 변경 — `JQ-minimal-skin-v1.1` → `daitnu-skin-v1.01`(폴더+zip). 폴더 rename이 로컬 프리뷰 서버(`bun run skin:serve`)의 파일 핸들 때문에 "Permission denied"로 막혀, 해당 프로세스만 정확히 찾아 종료 후 재시도해 해결(무관한 다른 bun/node 프로세스는 안 건드림). 프로젝트 전역에서 이 폴더명을 참조하던 18개 파일(CLAUDE.md/요구사항 문서 2종/검증 리포트/README/memory)의 문자열을 일괄 갱신, git이 rename으로 정확히 인식(커밋 `c18f28f`). `index.xml`의 `<name>JQ.Minimal</name>`·`<author>`(Jeeqong) 등 원작 라이선스 귀속 정보는 의도적으로 보존(재배포 금지 조항상 저작권 표시 유지 필요 — 우리가 붙인 폴더명과 원작이 자체 선언한 스킨명/저작자 정보는 별개) | daitnu-skin-v1.01/(rename), daitnu-skin-v1.01.zip(rename), 18개 참조 파일, memory/design-blog-harness.md | "JQ-minimal-skin-v1.1의 이름을 daitnu-skin-v1.01로 변경, 이 Design_Blog폴더에 있는 모든 내용을 변경" 요구사항 |
| 2026-09-02 | 대시보드 스킨 사이드바 — "둘러보기"(홈/태그/방명록) 그룹 전체 제거. 헤더 우측 버튼 3개와 완전히 중복이라 정리 요청. 검색 바로 아래 구분선만 남기고 아카이브(Design/Ai)로 바로 이어짐. `initActiveState()`(sidebar.js)는 DOM을 매번 다시 훑는 방식이라 손댈 필요 없이 그대로 정상 동작함을 확인. 헤더 버튼 3개 옆 주석("사이드바 둘러보기 그룹 재사용")도 갱신(그 그룹이 이제 없으므로). Tailwind 재빌드 + 로컬 서버로 실제 렌더링 확인 | dashboard-skin/skin.html | "사이드메뉴에 둘러보기 + 하위 홈,태그,방명록 제거" 요구사항 |
| 2026-09-05 | 스킨 소스 주석 금지 규칙을 하네스에 명시 — HTML/CSS/JS에는 `<!--`/`/*`/`//`를 달지 않고 짝 `{파일}.md`에만 적는다. 에이전트·스킬·오케스트레이터·Cursor 규칙에 동일 조항. 이유: 업로드 산출물이 지저분해지고, 주석 속 `<s_*>` 문자열이 목업 생성 정규식을 깨뜨린 실측이 있음 | agents/tistory-skin-developer.md, skills/{tistory-skin-development,tistory-skin-orchestrator}/SKILL.md, .cursor/rules/dashboard-skin-companion-comments.mdc | "클로드가 자꾸 주석다는거 같은데 주석을 각 파일의 md에 넣을 수 있게끔 하네스구성에 규칙 추가해" |
| 2026-09-05 | Playwright를 클로드와 같이 에이전트가 직접 등록·실행하도록 하네스에 명시. Cursor에 `@playwright/mcp` 등록(프로젝트 `.cursor/mcp.json` + 사용자 mcp.json). 브라우저 없으면 `playwright install` 또는 시스템 Chrome. 검증을 사용자에게 넘기지 않음 | skills/tistory-skin-development/SKILL.md, agents/tistory-skin-developer.md, skills/tistory-skin-orchestrator/SKILL.md, .cursor/{mcp.json,rules/dashboard-skin-playwright.mdc} | "클로드는 알아서 플레이와치로 등록까지 다 해주던데" |
| 2026-09-05 | 대시보드 스킨 `index.xml` 신설 — 스킨 편집 화면에 남아 있던 JQ.Minimal/Jeeqong/torytis 잔재를 DAITNU(제작자 jeongsanghoon@naver.com, 저작권 제작자)로 교체. 미리보기 jpg 3장과 함께 신규 등록용 zip 구성. 1차 `daitnu-skin-v1.01` 원작 귀속은 그대로 둠 | dashboard-skin/{index.xml,style.css,deploy/} | "스킨정보가 과거의 잔재가 남아있어 내용맥락은 바꿔서 만들어 스킨 새로 등록할꺼야" |
| 2026-09-06 | 웹폰트 미리보기 클래스에서 눈누/`noonnu` 제거 — `noonnu-live-sample(-card)` → `font-live-sample(-card)`. 초안 5건과 작성·발행 스킬에 동일 규칙 기록. CDN 원본 URL(`projectnoonnu`)은 그대로 | `_workspace/noonnu-drafts/*.html`, skills/{design-resource-curation,tistory-publishing}, agents/design-content-writer | "클래스명도 '눈누' 언급 금지" |
| 2026-09-07 | 폰트 썸네일 중앙정렬 버그 근본 수정(본고딕/본명조) — (1) Tistory/Daum CDN이 "새 글 최초 발행" 시 첨부한 대표이미지만 16:9로 강제 패딩하며 좌상단 앵커로 축소 배치(우리 PNG 자체는 정중앙이었음을 픽셀 분석으로 확인)하는 것을 R{width}x0 실측으로 확정 — "기존 글 대표이미지 교체"(삭제 후 재업로드) 경로는 이 패딩이 없어 그 절차로 재적용해 해결, 앞으로 매 발행 후 필수 검증 절차로 진행 기록에 남김. (2) 단일 글 상세의 대표이미지가 그리드카드 전용 규칙(aspect-ratio 5/4, 명시도 더 높음)에 밀려 16:9 대신 5:4로 크롭되던 실제 회귀도 함께 수정. 사이드바 카테고리 메뉴를 정적 하드코딩(2026-09-01 Q2=A 결정)에서 실제 관리자 카테고리 데이터 연동으로 전면 교체 — `[##_category_list_##]`를 실측(깨끗한 ul/li/a 트리, `new_ico_5.gif`로 신규글 자동 판정)해 신규 `category.js`가 숨겨둔 원본을 파싱해 기존 아코디언/배지/툴팁 data-slot 마크업을 런타임 합성(sidebar.css/js·tooltip.js 무변경 재사용). 관리자에서 "Code" 카테고리가 이미 있었는데 하드코딩 목록엔 없던 실제 불일치를 발견해 해결, 실제로 테스트 카테고리를 추가·삭제해 자동 반영을 실측 검증. "N" 배지를 텍스트 칩에서 8×8px 도트로 축소, 카테고리 아이콘을 전부 공용 폴더 아이콘(펼침/접힘 스왑)으로 통일 | dashboard-skin/components/{content.css,content.css.md,category.js(신규),category.js.md(신규),sidebar.css,sidebar.css.md}, dashboard-skin/{skin.html,skin.html.md,README.md,style.css}, dashboard-skin/tools/make-preview.mjs, dashboard-skin/deploy/*, _workspace/01_noonnu-batch-progress.json | "썸네일이 또 중앙이 아니야... 앞으로 같은 이슈 발생하지 않도록해", "사이드메뉴의 메뉴가 하드코딩되어있어 실제 관리자페이지의 카테고리관리가 연결되야돼", "N 부분 텍스트 제거 도트로만 표시", "전체 카테고리 공용(폴더) 아이콘 하나로 통일 / 하위메뉴 펼쳐졌을때 펼쳐진 아이콘으로" 요구사항 |
| 2026-09-07 | 사이드바 카테고리 배지 후속 조정 — "하위메뉴가 있을 시 1차메뉴에는 도트제거"에 따라 `category.js`의 아코디언(자식 있는) 분기에서만 배지 렌더링을 뺌(리프/하위 항목은 유지). 재배포 중 실측 발견: 파일업로드 탭에서 JS만 단독 재업로드해도 `skin.html`이 참조하는 `?_version_=` 캐시버스터 번호가 안 바뀌어 CDN이 옛 내용을 계속 서빙하는 함정을 확인 — HTML 탭에서 의미 없는 변경(끝에 빈 줄 추가)으로 "적용"을 한 번 더 눌러 새 버전 번호를 발급받아 우회, 실사이트에서 최종 반영 확인. 이 운영 함정을 `category.js.md`/`deploy/README.md`에 기록 | dashboard-skin/components/{category.js,category.js.md}, dashboard-skin/{skin.html,deploy/*} | "하위메뉴가 있을 시 1차메뉴에는 도트제거", "하위메뉴가 있을 시 1차메뉴 링크x 아코디언 기능 추가 디자인시스템 활용"(기존 구현이 이미 충족하고 있음을 재확인) |
| 2026-09-07 | 사이드바 아코디언 실사이트 미작동 버그(진짜 원인) 수정 — "아코디언기능 작동안함, 보고하기전에 기능검토도해" 재확인 요청으로 실사이트를 직접 클릭 테스트해 재현. 실측 결과 `sidebar.js`의 실사이트 배포본이 8553바이트(로컬 9184바이트)로 `initCollapsibleMenus` 함수 자체가 없는 오래된 버전이었음을 발견 — 이전에 그 함수를 추가·업로드했다고 믿었던 순간, `skin.html`(HTML 탭) "적용" 시점과 `sidebar.js` 파일업로드 시점이 어긋나 `?_version_=` 캐시버스터 번호가 옛 내용으로 CDN에 굳어버린 것(직전 커밋에서 category.js에 겪었던 것과 동일 함정, 이번엔 사후에야 sidebar.js에도 있었음을 인지). 재업로드(9.2kB 확인) + HTML 탭 무의미 변경으로 "적용" 재실행해 새 버전 번호 발급 → 실사이트에서 Design/Code 두 아코디언을 실제로 클릭해 `data-state` open↔closed 토글, 다른 아코디언과 독립적으로 동작함을 확인 후 종결. 검증 도중 사용자가 실제로 admin에서 Design에 "Mockup", Code에 "depth2" 하위 카테고리를 추가해 테스트해 둔 상태였음을 발견 — 두 카테고리 모두 아코디언으로 정상 전환됐다(정적 하드코딩이었다면 반영 안 됐을 변화). 교훈을 `sidebar.js.md` §15에 추가 기록 | dashboard-skin/{components/sidebar.js.md,skin.html,deploy/*} | "아코디언기능 작동안함. 보고하기전에 기능검토도해" |
| 2026-09-07 | 사이드바 카테고리 아코디언에 접기/펼치기 트랜지션 + 단일 열림(다른 항목 자동 접힘) 추가 — "디자인시스템 아코디언이랑 똑같이 만들으라고" 요청으로 사용자가 지정한 실제 Design-system 문서 사이트(`jeongsh90.github.io/portfolio-2026/Design-system#accordion`)의 진짜 Accordion 컴포넌트를 Playwright로 열어 실제 CSS(`@keyframes accordion-down/up`, `--accordion-content-height` CSS 변수)와 JS(`setAccordionItemState`/`initAccordions`, `data-type="single"`+`data-collapsible` 기본값) 소스를 직접 fetch해 그 메커니즘을 1:1로 이식(슬롯 이름은 이 프로젝트 사이드바가 이미 쓰던 `collapsible`/`sidebar-menu-sub` 체계 유지, 편차는 문서화). `category.js`에 패딩 없는 `sidebar-menu-sub-wrap` 래퍼를 추가해(원본이 `accordion-content`/`accordion-content-inner`로 2단 구조인 이유와 동일 — `box-sizing:border-box`에서 padding 있는 요소는 `height:0`으로 완전히 안 접힘) 실측 `scrollHeight`를 토글 직전 CSS 변수로 세팅해 애니메이션. `sidebar.js`의 `initCollapsibleMenus`를 재작성해 가장 가까운 공통 `[data-slot="sidebar-menu"]` 조상 단위로 묶어 그 안에서만 단일 열림을 강제(다른 그룹과 간섭 없음). 최초 렌더 시 자식이 있는 카테고리 중 첫 번째만 열리게(`firstExpandableIndex`), 나머지는 닫힘. 실사이트에서 Design→Code 클릭 시 Design이 자동으로 닫히고 Code가 열리는 것, 도로 Code를 눌러 완전히 접히는 것까지 전부 실측 확인 | dashboard-skin/components/{category.js,category.js.md,sidebar.js,sidebar.js.md,sidebar.css,sidebar.css.md}, dashboard-skin/{tools/make-preview.mjs,skin.html,deploy/*} | "아코디언에 접기,펼치기 트랜지션 추가, 열린페이지 제외하고 다른 리스트 접히게끔", "디자인시스템 아코디언이랑 똑같이 만들으라고" |
| 2026-09-07 | 사이드바 카테고리 아코디언 초기 열림 항목 수정 — "Code>depth2 페이지일때 아코디언이 해당하는것에 열려있어야되는데 지금 첫번째 목록이 열려있어" 지적. 직전 커밋의 `firstExpandableIndex`(항상 첫 번째 확장형 카테고리를 여는) 고정 로직이 실제 방문 페이지와 무관해 틀렸음을 확인 — `category.js`가 `window.location.pathname`을 `sidebar.js`의 `initActiveState()`와 동일한 방식으로 정규화한 뒤, 각 최상위 카테고리 자기 href와 접두어 비교(`pathIsUnder`, Tistory 카테고리 URL이 계층적이라 부모 href 하나로 모든 하위 카테고리 페이지를 커버)해 현재 페이지가 속한 카테고리를 찾아 그것을 연다. 일치하는 카테고리가 없는 페이지(홈 등)는 기존 첫 번째 폴백 유지. 실사이트에서 `/category/Code/depth2`(Code 열림+Design 닫힘), `/category/Design/Font`(Design 열림+Code 닫힘) 양쪽 다 실측 확인 | dashboard-skin/components/{category.js,category.js.md} | "Code>depth2 페이지일때 아코디언이 해당하는것에 열려있어야되는데 지금 첫번째 목록이 열려있어, 열링 페이지의 리스트가 열려있어야돼" |
| 2026-09-07 | 사이드바 카테고리 아코디언 — 무관한 페이지에서의 "첫 번째 카테고리 강제 열림" 폴백 완전 제거. "'Ai' 메뉴처럼 하위메뉴가 없을시 클릭하면 첫번째 리스트가 열려있음" 지적 — 직전 커밋에서 "일치하는 카테고리가 없으면 첫 번째로 폴백"하게 남겨뒀던 게 리프 카테고리(Ai)를 볼 때도 무관한 Design을 열어버리는 실제 버그였다. `category.js`에서 `firstExpandableIndex` 폴백 자체를 삭제 — 일치하는 확장형 카테고리가 없으면(`openIndex = -1`) 아무것도 기본으로 열리지 않는다(전부 접힌 채 시작). `/category/Ai`(둘 다 닫힘), 홈(둘 다 닫힘), `/category/Code/depth2`(Code만 열림) 전부 실사이트에서 재확인 | dashboard-skin/components/{category.js,category.js.md} | "'Ai' 메뉴처럼 하위메뉴가 없을시 클릭하면 첫번째 리스트가 열려있음" |
| 2026-09-07 | v1.0.1 이후 업데이트 공지사항 초안 등록 — 배포 준비 중 `dashboard-skin/deploy/`가 소스와 어긋나 있던 것 2건 발견·수정: `header.js`(줄바꿈 차이뿐인 사소한 불일치, 소스로 재동기화), `index.xml`(`<license>` 문구가 서로 다름 — 이 프로젝트 전반의 관례대로 `components`/루트 소스 쪽을 정본으로 삼아 `deploy/`를 덮어씀). `index.xml`의 `<version>`을 1.0.0→1.0.2로 올리고(공지 첨부파일이 실제로는 `DAITNU-v1.0.1.zip`이었던 기존 오프바이원을 여기서부터 맞춤), 소스 전체를 재검증 동기화한 뒤 `DAITNU-v1.0.2.zip`(index.xml/skin.html/style.css/미리보기 3장 + images/{tailwind.css,category.js,tooltip.js,sidebar.js,header.js,content.js,smooth-scroll.js})을 새로 빌드. `/manage/notice`에 v1.0.1 이후 실제 변경사항(카테고리 관리자 실시간 연동, 아코디언 전환)만 요약한 공지("DAITNU 스킨 v1.0.2 업데이트 안내")를 작성하고 새 zip을 첨부, 발행을 시도했으나 **"하루에 새롭게 공개 발행할 수 있는 글은 최대 15개까지입니다" 플랫폼 제한에 걸림**(같은 날 진행된 눈누 폰트 아카이브 대량 발행이 원인으로 추정) — 내용을 잃지 않도록 비공개로 저장(`/notice/31`). 공개 전환은 일일 한도가 초기화된 뒤 남은 작업으로 보류 | dashboard-skin/{index.xml,deploy/*,deploy/DAITNU-v1.0.2.zip(신규)} | "공지사항 1.0.1 버전이후 업데이트 내용 글 등록해" |
| 2026-09-07 | Ai>Skill 카테고리에 "Ruflo"(Claude Code용 에이전트 메타-하네스) 소개 글 등록 — 사용자가 처음 제시한 참고 링크(ruflo.biz)를 Playwright로 직접 열어보니 사용자가 설명한 내용(완전 무료, 60개+ 에이전트, 퀸 에이전트, 공유 메모리, 토큰 절반 절약)과 실제 사이트 내용(월 $69 유료 SaaS, 에이전트 4개뿐, 크립토/외환 제휴 마케팅 다수 포함)이 크게 달라 AskUserQuestion으로 확인 — 사용자가 실제 오픈소스 저장소(`github.com/ruvnet/ruflo`)를 재확인해줘 이를 1차 출처로 전환. WebFetch로 이 저장소 README 원문을 직접 대조(Claude Flow의 후속 리브랜딩, MIT 라이선스, 100+ 에이전트, Queen-led 계층구조, HNSW 벡터 메모리 공유, SONA 자기학습, 89% 정확도 지능형 라우팅 등 사용자가 설명한 특징과 정확히 일치함을 확인)해 `ai-prompt-skill-archive` 스킬의 검증 규칙(1차 출처 인용, 모델/도구명 명시, 확인 시점 기재, 미검증 항목 명시)에 따라 소개/설치·설정방법/사용방법 구성으로 작성. 설치 명령어(`npx ruflo@latest init wizard` 등)는 README 원문 그대로 인용했으나 이 환경에서 직접 실행해 검증하지는 않았음을 "검증 기록" 절에 명시. `posts/2026-09-07-ruflo-claude-code-agent-harness/`(final.html+meta.md)로 산출물 정리 후 Playwright로 실제 `/manage/post`에 카테고리 Ai>Skill·태그 8개 지정해 등록 — 당일 공개 발행 한도(15개) 소진 상태라 사용자 요청대로 비공개 저장(`daitnu.tistory.com/32`) | posts/2026-09-07-ruflo-claude-code-agent-harness/{final.html,meta.md}(신규) | "카테고리 추가했거든 ai > skill에 글 등록해 (글등록 제한때문에 일단 비공개)" + 소개/설치·사용방법 내용 제공, 참고 링크 재확인(ruflo.biz → github.com/ruvnet/ruflo) 응답 |
| 2026-09-07 | 글 본문 `word-break: keep-all`이 실사이트에서 안 먹히던 버그 수정 — "content.css에 .contents_style때문에 적용안됨" 제보로 실사이트 DOM을 직접 열어 확인한 결과, 스킨과 무관하게 **티스토리 자체가** 저장된 글 본문을 `<div class="tt_article_useless_p_margin contents_style">`로 한 번 더 감싸고, 그 클래스에 티스토리 플랫폼 자체 스타일시트(`tistory_admin/userblog/.../static/style/content.css` — 이 프로젝트의 동명 파일과는 완전히 다른 파일)가 `word-break: break-word`를 직접 선언해 두고 있었다(word-break는 상속 속성이라 우리 컨테이너의 keep-all이 내려가야 정상이지만, 이 wrapper 자신에 직접 선언된 값이 상속값을 이긴다). `[data-slot="post-single-body"] .contents_style`(명시도 0-2-0)로 티스토리의 `.contents_style`(0-1-0)보다 한 단 높여 덮어쓰고, 표 셀도 같은 이유로 `.contents_style table td` 규칙을 동일 패턴으로 재정의. `style.css` 재빌드 후 스킨 편집 CSS 탭에 붙여넣어 "적용", 실사이트(공개글 `/17`, 비공개 Ruflo 글 `/32` 둘 다)에서 `getComputedStyle`로 word-break가 keep-all로 바뀐 것을 실측 확인 | dashboard-skin/components/{content.css,content.css.md}, dashboard-skin/{style.css,deploy/style.css} | "블로그 글내용 속성 추가 워드 백 킵 올"(word-break:keep-all 추가 요청, "style.css에 등록" 확인 응답) → "content.css에 .contents_style때문에 적용안됨" 재제보 |
| 2026-09-07 | Ruflo 글(`/32`)에 대표이미지 추가 + 본문 가독성 재설계. (1) 사용자가 요청한 "Ruflo 로고"는 별도 벡터 로고가 없어(저장소 `assets/` 폴더 확인 결과 `ruFlo.png`/`ruflo-small.jpeg` 2개뿐, 둘 다 README 상단 배너로 쓰이는 동일한 네온사인 사진) 이 공식 배너 이미지를 그대로 가져와 1200×630 다크 캔버스(#171717) 중앙에 배치 + 하단에 부제 텍스트를 얹은 썸네일을 로컬 HTML(Playwright 스크린샷, 눈누 폰트 글 썸네일과 동일한 1200×630 방식 재사용)로 제작, 이미 발행된 글의 "대표이미지 추가"(수정 화면의 발행 다이얼로그)로 첨부 — 신규 발행이 아니라 기존 글 수정이라 앞서 겪었던 CDN 16:9 강제 패딩 버그 경로를 타지 않음을 실측 확인. (2) "가독성이 너무 없어 여백·색상구분·카드형태 활용해서 디자인시스템 적용" 요청에 따라 본문을 일반 문단·불릿 목록 위주에서 스킨이 이미 노출 중인 CSS 커스텀 프로퍼티(`var(--color-card)`, `var(--color-link)`, `var(--color-muted)`, `var(--radius-lg)` 등, `dashboard-skin/src/input.css`의 `:root`/`.dark` 토큰)를 인라인 스타일로 직접 참조하는 구조로 재작성 — "핵심 기능" 8개 항목을 반응형 카드 그리드(`grid-template-columns:repeat(auto-fit,minmax(260px,1fr))`)로, 설치 방법 A/B를 파란 알약 배지가 붙은 카드로, 웹 UI 2종을 링크 카드로, "참고·검증 기록"을 `--color-muted` 배경의 note 박스로 분리했다. 스킨 CSS를 건드리지 않고 이 글의 HTML(`final.html`)에만 인라인 style로 적용해 다른 글에 영향이 없게 함 — 티스토리 TinyMCE가 HTML 붙여넣기 후에도 인라인 style 속성을 그대로 보존함을 확인(에디터 자체 미리보기는 스킨 CSS 변수를 못 읽어 무채색으로 보이지만, 실제 라이브 페이지는 skin.html의 style.css를 로드하므로 정상 렌더 — 편집 화면과 실제 글 화면을 헷갈리지 않도록 반드시 실사이트에서 재확인). Playwright로 `/32` 실제 페이지의 Lenis 스크롤 컨테이너(`.scroll-fade-y.lenis`, `window.scrollBy`가 안 먹혀 `scrollTop` 직접 조작)를 스크롤하며 카드 그리드·배지·note 박스가 의도대로 렌더되는 것을 스크린샷으로 확인 | posts/2026-09-07-ruflo-claude-code-agent-harness/{final.html,attachments/thumbnail.png(신규)} | "Ruflo 로고로 썸네일 만들어", "방금등록한 글 내용을보면 가독성이 너무 없어 여백, 색상구분, 카드형태(필요 시)등 디자인시스템 활용해서 다시 적용해" |
| 2026-09-07 | Ruflo 글에 "일반적인 에이전트 하네스 구성과 무엇이 다른가" 섹션 신규 추가 — Claude Code가 원래 지원하는 "직접 구성하는 하네스"(서브에이전트 `.claude/agents/*.md` + 스킬 `SKILL.md`를 프로젝트마다 직접 작성)와 Ruflo가 제공하는 사전 패키지형 하네스를 6개 항목(에이전트 준비/작업 배분/세션 간 기억/학습/여러 에이전트 협업/시작 비용)으로 비교하는 `<table>`을 소개 섹션 뒤에 신설. 사용자의 다른 비공개 프로젝트(D:\MyCloud) 세부사항은 노출하지 않고 Claude Code 공개 기능(서브에이전트·스킬 파일 체계)만 근거로 "일반적인 직접 구성" 쪽을 설명, 어느 쪽이 우월하다는 주장 없이 트레이드오프로 마무리. 실사이트에서 표(헤더 음영, 테두리, 반응형 폭)가 스킨의 기존 table 스타일(content.css)로 별도 커스텀 없이 정상 렌더되는 것을 확인 | posts/2026-09-07-ruflo-claude-code-agent-harness/final.html | "Ruflo — Claude Code를 멀티 에이전트 팀으로 바꿔주는 무료 오픈소스 하네스 일반 하네스구성과 차이점내용도 넣어" |
| 2026-09-07 | 사이트 전체 문단 여백이 티스토리 자체 `!important`에 눌려 있던 버그 수정 + Ruflo 글 카드 2건 추가 회귀 수정. "블로그 글내용 속성 추가 워드 백 킵 올" → "content.css에 .contents_style때문에 적용안됨" → "속성때문에 가독성이 안좋아보임" 3연속 제보를 실사이트 `<p>` `getComputedStyle`로 직접 찍어 확인한 결과, `.contents_style`을 감싸는 티스토리 wrapper div가 실제로는 `class="tt_article_useless_p_margin contents_style"`로 클래스 2개를 함께 갖고 있었고, 그중 `tt_article_useless_p_margin`쪽 플랫폼 CSS(`.tt_article_useless_p_margin p { margin-top:0 !important; margin-bottom:0 !important; ... }`)가 **모든 글의 모든 `<p>` 태그**에서 세로 리듬을 완전히 지우고 있었다 — `!important`는 일반 선언은 물론 인라인 `style=""`보다도 강해, 일반 CSS로는 절대 이길 수 없다. `content.css`의 `:is(p,ul,ol,dl)`·`(h1~h6)+*`·`>:first-child` margin-top 규칙 3곳에 `!important`를 추가해 스킨 CSS 탭에 재적용, 공개글(`/17`)에서 `<p>` margin-top이 0px→16px로 정상화됨을 실측 확인. 이어서 사용자가 Ruflo 글의 실제 라이브 HTML을 그대로 붙여 보여준 "flo.ruv.io/goal.ruv.io 카드가 그리드 처리 안 됨" 제보로 두 번째 버그 발견 — `<a style="display:block">`로 카드 전체를 감싸던 마크업이 TinyMCE HTML→기본모드 변환 과정에서 `<a>` 래퍼째로 통째 증발해(원인은 미확정, TinyMCE가 블록 레벨 스타일이 걸린 `<a>`를 안전하지 않다고 판단해 벗겨내는 것으로 추정) 카드 4개 텍스트가 grid 컨테이너의 개별 항목으로 뿔뿔이 흩어져 있었다 — `<a>`를 카드 wrapper로 쓰지 않고 `<div>` wrapper + 제목 텍스트에만 인라인 `<a>`를 앵커하는 방식으로 교체. 같은 시점에 위 `!important` 인라인 스타일 무력화 문제가 이 글의 카드 내부 `<p>`(margin:0 등 커스텀 여백)에도 그대로 적용됨을 인지해, 카드 내부 텍스트 요소를 전부 `<p>`→`<div>`로 바꿔 티스토리 규칙의 태그 스코프(`p`만 지정) 밖으로 옮김 — 두 수정을 한 번에 반영해 재업로드, 실사이트에서 카드 배경·테두리 복원과 문단 간격 정상화를 함께 확인 | dashboard-skin/components/{content.css,content.css.md}, dashboard-skin/{style.css,deploy/style.css}, posts/2026-09-07-ruflo-claude-code-agent-harness/final.html | "블로그 글내용 속성 추가 워드 백 킵 올" → ".tt_article_useless_p_margin p 속성때문에 가독성이 안좋아보임" + 실제 라이브 HTML 붙여넣기로 "이부분 그리드처리 x" 제보 |
| 2026-09-07 | Ruflo 글의 "미검증 항목"을 실제 검증으로 승격 — 사용자 요청으로 `npm install -g ruflo@latest`를 이 환경에 실제로 전역 설치(397개 패키지, 정상 완료)하고 `ruflo --version`(v3.38.21)·`ruflo --help` 실행까지 확인, `agent`/`swarm`/`memory`/`hive-mind`(Queen 주도 합의 기반 조율)/`hooks`(자기학습) 등 실제 CLI 명령어 구조가 README가 설명하던 기능과 일치함을 실측했다. 글의 "참고·검증 기록" 절을 이 내용으로 갱신 — 다만 `ruflo init`으로 실제 프로젝트를 초기화해 에이전트를 구동하는 것까지는 하지 않음(이 저장소는 Ruflo와 무관한 별도 프로젝트라 `.claude/`·`CLAUDE.md` 등 관련 파일 생성을 의도적으로 생략, 에이전트 자동 라우팅 정확도 89% 등 구체 수치는 여전히 공식 문서 주장값으로 명시 유지) | posts/2026-09-07-ruflo-claude-code-agent-harness/{final.html,meta.md} | "npm install -g ruflo@latest 설치해" |
