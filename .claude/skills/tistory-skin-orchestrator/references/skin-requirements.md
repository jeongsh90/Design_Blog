# 티스토리 스킨 요구사항 원문 정리 (1차 — daitnu-skin-v1.01 리스킨, SUPERSEDED)

> ⚠️ **2026-09-01 이 문서 대부분은 폐기됐다.** 사용자가 "기존 잔재 속성 모두 제거하고" 대시보드 느낌의 shadcn/ui 방식으로 새로 만들기로 방향을 바꿨다 — 새 작업의 유일한 근거 문서는 **`dashboard-shadcn-requirements.md`**(같은 폴더)다. 이 문서는 그대로 두되(감사 이력 보존 + Tistory 템플릿 태그 문법·index.xml 구조에 대한 지식은 여전히 유효해 참고 가치가 있음), **더 이상 이 문서를 기반으로 작업을 진행하지 않는다.** `skin/`, `skin.zip`, `_workspace/10_*`, `_workspace/11_*`(daitnu-skin-v1.01 리스킨 1차 산출물)도 삭제하지 않고 보존하지만 새 작업의 베이스가 아니다.

사용자가 2026-09-01에 두 차례 메시지로 전달한 요구사항을 섹션별로 정리한 것(1차 리스킨 방향, 현재는 폐기). skin-designer/skin-developer는 이제 이 문서 대신 `dashboard-shadcn-requirements.md`를 읽는다.

## ⚠️ 중요: 이 작업은 "만들기"가 아니라 "리스킨(재도색)"이다

`D:\MyCloud\Design_Blog\daitnu-skin-v1.01`에 **이미 완성된 티스토리 스킨 패키지**가 있다(`skin.html`/`style.css`/`script.js`/`index.xml`, 표준 티스토리 스킨 구조). 사용자가 앞서 나열한 요구사항 대부분은 이 스킨이 **이미 구현하고 있는 기능/옵션을 사용자가 참고해 설명한 것**으로 확인됐다 — 실제로 index.xml에 와이드 배너(height:250px)/리스트·카드(3:2)·썸네일(3:4) 목록타입/프로필사진/로고(24px·160px) 옵션이 이미 정의돼 있고, `skin.html`은 `<html class="dark">`로 다크모드 토글 구조를 이미 갖췄으며, `script.js`에 theme/toc/progress(스크롤 인디케이터로 추정)/prev-next 관련 로직이 이미 있다.

**사용자의 최신 지시("여기에 있는 컴포넌트 내용을 Design-system로 교체")는 이 기존 패키지를 기반으로, 그 시각적 스타일(컬러/타이포/spacing/컴포넌트 디자인)만 `D:\MyCloud\2026포트폴리오\Design-system` 토큰으로 교체하라는 뜻이다.** 즉:
- **하지 말 것:** skin.html의 티스토리 템플릿 태그 구조나 index.xml의 옵션 체계, script.js의 기능 로직을 처음부터 새로 설계하는 것 (이미 동작하는 것을 갈아엎을 근거가 없다)
- **할 것:** style.css(및 필요 시 skin.html의 클래스/마크업, script.js의 값)를 감사(audit)해 Design-system 토큰 체계로 재도색

### 베이스 스킨 실측 감사 결과 (하네스 구성 시점)

- `style.css` 108KB, **CSS 커스텀 프로퍼티(`--` 변수)가 0개** — 색상/치수가 전부 하드코딩. `.dark ` 선택자가 50회 등장 — 다크모드를 변수 스위칭이 아니라 라이트/다크 규칙을 선택자마다 중복 선언하는 방식으로 구현했다. 리스킨 시 이걸 Design-system처럼 `:root`+`.dark` 토큰 변수 구조로 정리하면 유지보수성도 함께 개선된다(사용자가 명시 요청하진 않았지만 "컴포넌트 교체"의 자연스러운 결과 — skin-developer 판단으로 진행하되 임의 기능 변경은 아님을 최종 보고에 명시).
- ⚠️ **정정 (최초 감사 오류):** 처음 조사 때 `style.css`/`skin.html`을 따로따로 grep하다가 놓쳤는데, **`skin.html`의 `<head>`가 이미 Pretendard 변수 폰트를 CDN으로 로드하고 있다** — `<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css">` (orioncactus/pretendard는 Pretendard 공식 저장소, `<head>`에 `preconnect`로 jsdelivr도 이미 걸어둠). 즉 "폰트가 적용 안 되고 있다"는 이전 감사 결론은 **틀렸다** — 폰트 자체는 정상 작동 중이다. `style.css`의 `font-family:Pretendard Variable, ...` 선언은 이 CDN 폰트와 정확히 매칭된다.
- 사용자가 별도로 `font-family:'Pretendard'; src:url('https://cdn.jsdelivr.net/gh/projectnoonnu/pretendard@1.0/Pretendard-Thin.woff2') format('woff2');` 형태(눈누 CDN, 정적 굵기별 woff2)도 제시했으나, **✅ 확정: 현행 유지 — `orioncactus/pretendard` 변수 폰트, jsdelivr CDN.** skin.html의 기존 `<link>`를 그대로 두고, Design-system/눈누 쪽으로 바꾸는 작업은 하지 않는다. 폰트 관련해서 리스킨 시 할 일은 없음(이미 정상 동작 중) — 다른 컴포넌트의 색/spacing/radius 토큰화에만 집중한다.
- `font-family: --value(--font-sans)`(1곳) — Tailwind v4 빌드 산출물이 컴파일되지 않은 채 그대로 남은 것으로 보이는 깨진 선언. 실제 브라우저에서 무효 처리되어 그 규칙만 조용히 폴백된다 — 리스킨 과정에서 발견 시 올바른 값으로 고친다.
- `font-family: 'Noto Serif KR'`(1곳) — 어딘가 세리프 폰트를 쓰는 지점이 있다(제목/커버 등 추정). `D:\MyCloud\Font\NotoSerifKR.zip`이 원본 소스로 존재하니 실제로 쓰기로 하면 그것도 self-host 처리한다.
- `index.xml` 라이선스: *"개인 사용 목적의 수정은 허용되며, 저작권 표시를 유지해야 합니다. 재배포 및 상업적 사용은 금지됩니다."* → **원작자(Jeeqong, jeeqong.tistory.com) 정보를 index.xml `<author>`에서 지우지 않는다.** 개인 블로그(daitnu.tistory.com)에 적용하는 것은 라이선스 범위 내지만, 이 리스킨 결과물을 공개 배포/재판매하지 않는다.
- `__MACOSX/`, `.DS_Store`는 zip 압축 해제 시 생긴 macOS 잔여물 — 최종 업로드 패키지에서 제외한다.

## 소스

- 블로그 주소: https://daitnu.tistory.com/
- 관리자 페이지: https://daitnu.tistory.com/manage
- **베이스 스킨(리스킨 대상 원본)**: `D:\MyCloud\Design_Blog\daitnu-skin-v1.01` — `skin.html`/`style.css`/`script.js`/`index.xml` 표준 티스토리 스킨 패키지. 위 감사 결과 참고.
- 디자인 시스템 소스: `D:\MyCloud\2026포트폴리오\Design-system` (Tailwind v4 컴파일 산출물, shadcn 계열 토큰, `.dark` 클래스 다크모드)
- 폰트: Pretendard — `D:\MyCloud\2026포트폴리오\Design-system\fonts\pretendard`(9종 굵기 self-host woff2, 즉시 사용 가능) / `D:\MyCloud\Font\Pretendard.zip`(원본 백업) / 세리프 필요 시 `D:\MyCloud\Font\NotoSerifKR.zip`

## 전역 / 레이아웃

- **반응형 3단**: PC / 태블릿 / 모바일. 해상도 767px 이하부터 모바일로 전환 — 이 지점부터 사이드바가 접힌다.
- **헤더의 블로그 메뉴는 모바일에서 사이드바(오프캔버스/드로어)로 이동**한다 — PC/태블릿에서는 헤더에, 모바일에서는 별도 열림 방식으로.
- **스크롤 인디케이터** 제공(페이지 읽기 진행률 표시).
- **테마설정 기능** — 사용자가 라이트/다크를 수동 전환할 수 있어야 하고, 기본값은 사용자 디바이스(OS) 설정을 따른다(시스템 다크/라이트 지원). 3-상태(시스템 따름 / 라이트 강제 / 다크 강제)로 구현하고 선택은 재방문 시에도 유지되어야 한다(로컬 저장).
- **하단 Top 버튼**(맨 위로 스크롤) 구현.
- **코드 하이라이팅** 지원(코드 축 글이 많은 블로그라 필수).
- **이전 글 / 다음 글 보기** 네비게이션(포스트 하단).

## 헤더

- 프로필 사진 노출.
- 로고 이미지 지원 — **높이 24px / 최대 가로 160px** 규격 준수.
- 티스토리 메뉴바/구독 — **이건 스킨 코드가 아니라 티스토리 자체 관리자 설정으로 켜는 기능**이다. 스킨 쪽에서는 그 UI가 자연스럽게 자리 잡을 공간만 마련하고, 실제 활성화는 사용자가 관리자 페이지에서 직접 설정해야 한다는 점과 설정 방법(공식 안내 링크)을 산출물에 포함한다.

## 리스트(카테고리 글 목록)

- **썸네일 형태 3종**: 사각형 / 원형 / 둥근 사각형 — 스킨 옵션으로 선택 가능해야 함.
- **목록 타입 3종**: 리스트형(텍스트 위주 가로 행) / 카드형 / 썸네일형.
- 리스트형은 **모바일에서 1칼럼(세로 스택)** 배치.
- 카드형·썸네일형은 **그리드**: PC 3열 / 태블릿 2열 / 모바일 1열.

## 커버 (홈 상단 배너)

- **와이드 배너 형태**, 제목 + 설명 포함.
- 여러 커버 항목이 **세로로 쌓이며**, 텍스트 배치가 항목마다 **교차(좌/우 지그재그)로 노출**된다.
- 디바이스(PC/태블릿/모바일)별 배너 미리보기가 필요 — 즉 데스크톱에서 보이는 형태와 모바일에서 보이는 형태가 다를 수 있음을 시안 단계에서 각각 보여줘야 한다.
- ⚠️ "텍스트가 교차노출"의 정확한 의도가 모호할 수 있다 — skin-designer는 좌/우 교대 정렬 배너 스택으로 1차 해석해 시안을 만들되, 시안 제시 시 이 해석이 맞는지 사용자에게 확인받는다.

## 본문(포스트 상세)

- **대표이미지 타입 2종**: default(기본 비율) / wide(와이드 비율). 글마다 어느 타입을 쓸지 선택 가능해야 함.

## 포스트 내 TOC(목차 자동 생성)

- 본문의 제목 태그를 스캔해 자동으로 목차를 만든다.
- **플로팅 버튼**으로 우측에 배치, 클릭 시 펼침/닫힘.
- 목차 항목은 **19개까지 그대로 노출**되고, 그 이상은 **내부 스크롤**로 본다(TOC 패널 자체의 max-height 제한 + overflow-y:auto).

## 미확정/확인 필요 사항

- 태블릿 브레이크포인트의 정확한 px 값은 사용자가 명시하지 않았다 — skin-designer가 관례값(768~1023px)을 기본으로 제안하고 시안에서 확인받는다.
- 커버 배너 "교차노출"의 정확한 의미(위 참고) — 베이스 스킨의 기존 배너 구현이 이미 이 의도대로 동작하는지 감사에서 먼저 확인하고, 아니라면 무엇이 다른지 구체적으로 짚는다.
- **베이스 스킨의 기존 기능이 요구사항을 실제로 충족하는지는 코드를 읽고 확인해야 한다 — script.js에 `theme`/`toc`/`progress`/`prev-next` 키워드가 존재한다는 것만으로 "요구사항 19개 항목 초과 시 스크롤" 같은 세부 동작까지 이미 맞다고 가정하지 않는다.** skin-developer가 실제 동작을 읽고 감사 보고서에 "이미 충족" / "존재하지만 세부 스펙과 다름(무엇이 다른지)" / "없음(새로 구현 필요)"으로 항목별 판정한다.
