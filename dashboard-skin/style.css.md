# `style.css` — 설계 주석

소스: `dashboard-skin/style.css`

티스토리 스킨 zip/등록에 필요한 루트 CSS 슬롯이다. 내용은 비워 둔다.

이 스킨의 실제 스타일은 HTML 편집 파일 업로드로 `./images/`에 올라가는
`tailwind.css`와 `components/*.css`가 담당한다. 루트 `style.css`에 규칙을 쌓지 않는다.
